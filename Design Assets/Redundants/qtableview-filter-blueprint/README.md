# QTableView + filtering blueprint (reference only)

Archived from commit `7b4b814` (tag: `qtableview-filter-attempt`).

These files are **not imported** by the application. Use them as guidance when manually rebuilding:

- `PandasTableModel` + `ViewDisplayController` + `QTableView`
- `FilterableHeaderView` (filter arrow on header, active-filter colouring)
- `ColumnFilterPopup` (Excel-style filter UI)

Rebuild baseline: commit `56bd1b3` (QTableWidget + column tree selection).
